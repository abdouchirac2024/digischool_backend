services:
  - type: web
    name: digischool-backend
    env: docker
    plan: free
    region: frankfurt
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DB_HOST
        fromDatabase:
          name: db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: db
          property: databaseName
      - key: DB_USERNAME
        fromDatabase:
          name: db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: JPA_DDL_AUTO
        value: update

databases:
  - name: db
    plan: free
    databaseName: digischool
    user: digischool
